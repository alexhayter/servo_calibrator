// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

#define QUAD_OUT_SET_CYCLES 10

// -------------- //
// quadrature_out //
// -------------- //

#define quadrature_out_wrap_target 0
#define quadrature_out_wrap 3
#define quadrature_out_pio_version 1

static const uint16_t quadrature_out_program_instructions[] = {
            //     .wrap_target
    0xe900, //  0: set    pins, 0                [9]
    0xe901, //  1: set    pins, 1                [9]
    0xe903, //  2: set    pins, 3                [9]
    0xe902, //  3: set    pins, 2                [9]
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program quadrature_out_program = {
    .instructions = quadrature_out_program_instructions,
    .length = 4,
    .origin = -1,
    .pio_version = quadrature_out_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config quadrature_out_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + quadrature_out_wrap_target, offset + quadrature_out_wrap);
    return c;
}

#include "hardware/clocks.h"
void quadrature_out_program_init(PIO pio, uint sm, uint offset, uint pin, float freq)
{
    pio_gpio_init(pio, pin);
    pio_gpio_init(pio, pin + 1);
    pio_sm_set_consecutive_pindirs(pio, sm, pin, 2, true);
    pio_sm_config c = quadrature_out_program_get_default_config(offset);
    sm_config_set_set_pins(&c, pin, 2);
    int cycles_per_bit = QUAD_OUT_SET_CYCLES * 4;
    float div = clock_get_hz(clk_sys) / (freq * cycles_per_bit);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}

#endif

